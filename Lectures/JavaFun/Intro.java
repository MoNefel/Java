package Lectures;

public class Intro {

    public static void main(String[] args) {
        System.out.println("Hello world");

        /*
         * 1./data types in java : 
         * **Primitive => int -> 10 , double -> 10.4567 , boolean -> false or true, char -> 'g'(need to declared in simple quote is not a string).
         * **Object (can be nullled) => Integer, Double, Long, String, Boolean, Character
         * we declare variables by -> datatype name = value*/
        //int num = 10; //when we use primitive type we don't have acces to the Integer object's methode
        //Integer num1 = 20; //and if we ues Object type we can use all the methods from that object
        // for example num1.ToString()

        /*2./ Operators:
         * Mathematical : => +, *, -, %, /
         * 
         * Comparison => <, >, ==, >=, <=
         * 
         * Logical => &&, ||, !
         * 
         * Conditionals => if(condition){
         * do this if true
         * }else{
         * do something else
         * }
         * 
         * Trenary => like in JS -> condition ? result if true : result if false
         * 
         * Loops -> for(int i =0; i<10; i++){
         * System.out.println(i)
         * }
         * 
         * Switch => 
         */

    }
}